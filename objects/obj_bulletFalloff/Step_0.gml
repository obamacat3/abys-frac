/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();
speed *= falloff
amplitude *= falloff
freq *= falloff
if speed < 0.05 instance_destroy()
